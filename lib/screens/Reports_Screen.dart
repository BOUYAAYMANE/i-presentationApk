import 'package:flutter/material.dart';

class ReportsScreen extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text('Créer une rapport'),
      ),
      body: Center(
        child: Text('Formulaire de création de session'),
      ),
    );
  }
} 