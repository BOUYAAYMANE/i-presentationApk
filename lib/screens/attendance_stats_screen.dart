import 'package:flutter/material.dart';

class AttendanceStatsScreen extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text('Créer une atte'),
      ),
      body: Center(
        child: Text('Formulaire de création de att'),
      ),
    );
  }
} 